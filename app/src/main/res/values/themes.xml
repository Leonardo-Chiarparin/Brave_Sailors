<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main application theme -->
    <style name="Theme.Brave_Sailors" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">

        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Usa il nuovo layer-list con dimensioni 768dp -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>

        <!-- Tema dopo lo splash -->
        <item name="postSplashScreenTheme">@style/Theme.Brave_Sailors</item>

        <!-- Rimuove ActionBar e Title anche nello splash -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>

    </style>
</resources>
